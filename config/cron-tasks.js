module.exports = {
  /**
   * Simple example.
   * Every monday at 1am.
   */

  // '*/2 * * * * *': ({ strapi }) => {
  //   // Add your own logic here (e.g. send a queue of email, create a database backup, etc.).
  //   console.log ('iyi')
  // },
  // '*/3 * * * * *': ({ strapi }) => {
  //   // Add your own logic here (e.g. send a queue of email, create a database backup, etc.).
  //   console.log ('naber')
  //   const users = strapi.query
  // },
};